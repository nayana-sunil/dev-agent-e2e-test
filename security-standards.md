# Security Standards

1. Never hardcode credentials, API keys, or secrets in source files — load them from environment variables or a secrets manager.
2. Always validate and sanitize external input before using it in a request or query.
