# Utils

Shared helper utilities used across the project.

- `API_KEY` — default API key constant.
- `get_default_timeout()` — returns the default request timeout, in seconds, used for outbound HTTP calls.

See `utils.py` for the implementation.
