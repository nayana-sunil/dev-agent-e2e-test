# Coding Standards — Python

1. Every public function must have a docstring.
2. Never use a bare `except:` — always catch a specific exception type.
3. Do not use `print()` for diagnostic output — use the `logging` module.
4. Module-level constants must be UPPER_SNAKE_CASE.
5. Every public function must have type hints on its parameters and return value.
